# Model weights

Put local model weights here during development, for example:

```text
backend/weights/low_dose_ct.pth
backend/weights/cbct.pth
backend/weights/mr_to_ct.pth
backend/weights/super_resolution.pth
```

Weight files are ignored by git in `.gitignore`.
